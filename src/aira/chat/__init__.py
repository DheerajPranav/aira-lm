"""Aira chat — the engine that connects memory to a generation backend.

Empty namespace at Step 01. The chat pipeline (guard → decide → mutate → retrieve →
compose → generate), the ``MockBackend`` and graceful degradation are implemented in
Step 08.
"""

__all__: list[str] = []
