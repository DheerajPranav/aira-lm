"""Aira Core — the compact decoder-only transformer.

Empty namespace at Step 01. The reversible byte tokenizer, transformer blocks,
training loop, checkpointing and generation are implemented in Steps 11–12. PyTorch
is an optional dependency (the ``core`` extra) and is not required before then.
"""

__all__: list[str] = []
